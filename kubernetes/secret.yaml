# ─────────────────────────────────────────────────────────────
# Secret: Vault token
# In production: use Vault Agent Injector instead
# Base64 encoded values
# echo -n "qtec-root-token" | base64
# ─────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: qtec-task-secrets
  namespace: qtec-task
  labels:
    app: qtec-task
type: Opaque
data:
  # base64 encoded "qtec-root-token"
  VAULT_TOKEN: cXRlYy1yb290LXRva2Vu